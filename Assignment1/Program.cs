using System;
using System.Collections.Generic;
using System.Text;
using System.Linq;



namespace Assignment1
{
    public class Program

    {


        public static void Main(string[] args)
        {
            StudentDisp();
            

        }

        private static void StudentDisp()
        {
            public StudentDisp1(string fullName, string StNumber, int Age, string Address);

            double sum = 0, AverageScore = 0;
            int[] scores = { 100, 66, 60, 90, 99, 88 };
            for (int i = 0; i < scores.Length; i++)
            {
                sum += scores[i];
            }
            AverageScore = sum / scores.Length;

            Console.WriteLine($"Full Name of the student: {fullName}");
            Console.WriteLine($"Student {fullName} score is {AverageScore}");
            Console.WriteLine($"Student {FullName} score is Living in {City}");
            Console.WriteLine($"Student {FullName} address is {FullAddress}");
            Console.WriteLine($"Student {FullName} address is {FullAddress}");


        }

    }
            
        
    }
}
